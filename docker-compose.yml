version: '3.3'
services:
  api:
    build: ./
    restart: unless-stopped
    networks:
    - flask
    ports:
    - 8080:8080

  db:
    image: nouchka/sqlite3
    volumes:
    - sqlite:/root/db

volumes:
  sqlite:
    external: true
networks:
  flask:
    external: true
